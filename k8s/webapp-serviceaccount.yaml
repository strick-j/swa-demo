---
# Service account the webapp pod runs as. Its (namespace, service-account) is the
# workload selector the SWA node-group policy matches when issuing the SPIFFE ID.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: swa-demo-webapp
  namespace: swa-demo
  labels:
    app: swa-demo-webapp
