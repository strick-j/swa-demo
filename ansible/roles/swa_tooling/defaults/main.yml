---
# SWA Helm charts are synced from the same S3 prefix as the images (upload the
# whole bundle there; images stay as *.tar, charts as *.tgz).
images_s3_uri: ""
aws_region: "us-east-1"
bundle_local_dir: "/home/{{ minikube_user }}/swa-bundle"
project_stage_dir: "/home/{{ minikube_user }}/swa-demo"
