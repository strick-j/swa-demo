---
# Pin kubectl to a version compatible with the SWA-supported k8s range (1.33-1.35).
kubectl_version: "v1.34.0"
# Helm release to install (extracted directly from get.helm.sh).
helm_version: "v3.21.1"
